/** Public MCP tool-surface version. Bump on breaking schema changes. */
export const SERVER_VERSION = "1.1.0";

/** Persistent OAuth-vault schema admitted by this runtime. */
export const OAUTH_VAULT_SCHEMA_VERSION = 1;
